name: grouprde
description: "GroupRide - Smart group riding & trip coordination for motorcycle riders."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  provider: ^6.1.2
  google_maps_flutter: ^2.9.0
  geolocator: ^13.0.1
  shared_preferences: ^2.3.2
  uuid: ^4.5.1
  intl: ^0.19.0
  qr_flutter: ^4.1.0

dev_dependencies:
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
